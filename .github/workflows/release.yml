name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.5'
          channel: 'stable'
          cache: true
      
      - name: Install dependencies
        run: flutter pub get
      
      - name: Build database
        shell: bash
        run: |
          # Download and extract Kanji API data
          curl -L -o tools/kanjiapi_full.zip https://kanjiapi.dev/kanjiapi_full.zip
          unzip -j tools/kanjiapi_full.zip kanjiapi_full.json -d tools
          dart run tools/json_to_sqlite.dart tools/kanjiapi_full.json lib/assets/jpn.db
          rm tools/kanjiapi_full.zip
          
          # Download and convert JMdict data
          curl -o tools/JMdict_e.gz ftp://ftp.edrdg.org/pub/Nihongo/JMdict_e.gz
          gzip -d tools/JMdict_e.gz
          dart run tools/jmdict_to_sqlite.dart tools/JMdict_e lib/assets/jpn.db
          rm tools/JMdict_e
      
      - name: Build Windows
        run: flutter build windows --release
      
      - name: Create archive
        run: |
          cd build/windows/x64/runner/Release
          7z a -tzip ../../../../../jpn-learning-diary-windows.zip .
      
      - uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: jpn-learning-diary-windows.zip

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.5'
          channel: 'stable'
          cache: true
      
      - name: Install dependencies
        run: flutter pub get
      
      - name: Build database
        run: |
          # Download and extract Kanji API data
          curl -L -o tools/kanjiapi_full.zip https://kanjiapi.dev/kanjiapi_full.zip
          unzip -j tools/kanjiapi_full.zip kanjiapi_full.json -d tools
          dart run tools/json_to_sqlite.dart tools/kanjiapi_full.json lib/assets/jpn.db
          rm tools/kanjiapi_full.zip
          
          # Download and convert JMdict data
          curl -o tools/JMdict_e.gz ftp://ftp.edrdg.org/pub/Nihongo/JMdict_e.gz
          gzip -d tools/JMdict_e.gz
          dart run tools/jmdict_to_sqlite.dart tools/JMdict_e lib/assets/jpn.db
          rm tools/JMdict_e
      
      - name: Build macOS
        run: flutter build macos --release
      
      - name: Create archive
        run: |
          cd build/macos/Build/Products/Release
          zip -r ../../../../../jpn-learning-diary-macos.zip "Japanese Learning Diary.app"
      
      - uses: actions/upload-artifact@v4
        with:
          name: macos-build
          path: jpn-learning-diary-macos.zip

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build libgtk-3-dev libsqlite3-dev libsqlite3-0 libasound2-dev
      
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.5'
          channel: 'stable'
          cache: true
      
      - name: Install dependencies
        run: flutter pub get
      
      - name: Build database
        run: |
          # Download and extract Kanji API data
          curl -L -o tools/kanjiapi_full.zip https://kanjiapi.dev/kanjiapi_full.zip
          unzip -j tools/kanjiapi_full.zip kanjiapi_full.json -d tools
          dart run tools/json_to_sqlite.dart tools/kanjiapi_full.json lib/assets/jpn.db
          rm tools/kanjiapi_full.zip
          
          # Download and convert JMdict data
          curl -o tools/JMdict_e.gz ftp://ftp.edrdg.org/pub/Nihongo/JMdict_e.gz
          gzip -d tools/JMdict_e.gz
          dart run tools/jmdict_to_sqlite.dart tools/JMdict_e lib/assets/jpn.db
          rm tools/JMdict_e
      
      - name: Build Linux
        run: flutter build linux --release
      
      - name: Create archive
        run: |
          cd build/linux/x64/release/bundle
          tar -czvf ../../../../../jpn-learning-diary-linux.tar.gz .
      
      - uses: actions/upload-artifact@v4
        with:
          name: linux-build
          path: jpn-learning-diary-linux.tar.gz

  build-android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install SQLite development libraries
        run: |
          sudo apt-get update
          sudo apt-get install -y libsqlite3-dev libsqlite3-0 libasound2-dev
      
      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.5'
          channel: 'stable'
          cache: true
      
      - name: Decode keystore
        run: echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode > android/upload-keystore.jks
      
      - name: Create key.properties
        run: |
          echo "storePassword=${{ secrets.ANDROID_KEYSTORE_PASSWORD }}" > android/key.properties
          echo "keyPassword=${{ secrets.ANDROID_KEY_PASSWORD }}" >> android/key.properties
          echo "keyAlias=${{ secrets.ANDROID_KEY_ALIAS }}" >> android/key.properties
          echo "storeFile=../upload-keystore.jks" >> android/key.properties
      
      - name: Install dependencies
        run: flutter pub get
      
      - name: Build database
        run: |
          # Download and extract Kanji API data
          curl -L -o tools/kanjiapi_full.zip https://kanjiapi.dev/kanjiapi_full.zip
          unzip -j tools/kanjiapi_full.zip kanjiapi_full.json -d tools
          dart run tools/json_to_sqlite.dart tools/kanjiapi_full.json lib/assets/jpn.db
          rm tools/kanjiapi_full.zip
          
          # Download and convert JMdict data
          curl -o tools/JMdict_e.gz ftp://ftp.edrdg.org/pub/Nihongo/JMdict_e.gz
          gzip -d tools/JMdict_e.gz
          dart run tools/jmdict_to_sqlite.dart tools/JMdict_e lib/assets/jpn.db
          rm tools/JMdict_e
      
      - name: Build Android APK
        run: flutter build apk --release
      
      - name: Rename APK
        run: mv build/app/outputs/flutter-apk/app-release.apk jpn-learning-diary-android.apk
      
      - uses: actions/upload-artifact@v4
        with:
          name: android-build
          path: jpn-learning-diary-android.apk

  create-release:
    needs: [build-windows, build-macos, build-linux, build-android]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true
      
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/jpn-learning-diary-windows.zip
            artifacts/jpn-learning-diary-macos.zip
            artifacts/jpn-learning-diary-linux.tar.gz
            artifacts/jpn-learning-diary-android.apk
          generate_release_notes: true
          draft: false
